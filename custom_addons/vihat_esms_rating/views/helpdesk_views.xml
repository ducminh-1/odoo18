<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="action_open_rating_views" model="ir.actions.act_window">
        <field name="name">Ratings</field>
        <field name="res_model">rating.rating</field>
        <field name="view_mode">kanban,list,graph,pivot,form</field>
        <field name="domain">[['res_model','=','helpdesk.ticket']]</field>
        <field name="context">{'create':0, 'edit': 0}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                No rating yet
            </p>
            <p>
                There is no rating for this object at the moment.
            </p>
        </field>
    </record>

    <menuitem
        id="helpdesk_rating"
        parent="helpdesk.menu_helpdesk_root"
        action="action_open_rating_views"
        name="Rating"
        sequence="10"
    />
</odoo>
